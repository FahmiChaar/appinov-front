import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appinov_test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
